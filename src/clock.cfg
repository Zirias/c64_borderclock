MEMORY {
  LDADDR: start = $bffe, size = 2;
  CODE: start = $c000, size = $280, fill=yes;
  SPRITES: start = $c280, size = $2c0;
  BSS: start = $c540, size=$20;
  SPPTR: start = $c7f8, size = 8;
}

SEGMENTS {
  LDADDR: load = LDADDR;
  CODE: load = CODE;
  DATA: load = CODE;
  BSS: load = BSS, type=bss, optional=yes;
  SPRITES: load = SPRITES;
  SPPTR: load = SPPTR, type=bss;
}

